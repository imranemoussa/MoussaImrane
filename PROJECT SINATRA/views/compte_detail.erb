<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails du Compte</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-lg">
        <h1 class="text-2xl font-bold text-center text-gray-800 mb-4">Détails du Compte</h1>
        
        <div class="space-y-2">
            <p class="text-lg"><strong>Numéro :</strong> <%= @compte["numero"] %></p>
            <p class="text-lg"><strong>Propriétaire :</strong> <%= @compte["nom"] %></p>
            <p class="text-lg"><strong>Solde :</strong> <%= @compte["solde"] %> FCFA</p>
            <p class="text-lg"><strong>Statut :</strong> <%= @compte["actif"] ? "Actif" : "Inactif" %></p>
        </div>

        <div class="mt-4">
            <h2 class="text-xl font-bold text-gray-800 mb-2">Historique des Transactions</h2>
            <% if @logs.nil? || @logs.empty? %>
                <p class="text-gray-600">Aucune transaction enregistrée.</p>
            <% else %>
                <ul class="list-disc pl-5">
                    <% @logs.each do |log| %>
                        <li class="text-gray-700"><%= log["operation"] %> de <%= log["montant"] %> FCFA</li>
                    <% end %>
                </ul>
            <% end %>
        </div>

        <div class="mt-6 flex justify-between">
            <a href="/comptes" class="bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-700 transition">
                Retour
            </a>
            <a href="/ajout_transaction/<%= @compte['numero'] %>" class="bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 transition">
                Déposer
            </a>
            <a href="/retrait_transaction/<%= @compte['numero'] %>" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 transition">
                Retirer
            </a>
        </div>
    </div>
</body>
</html>
